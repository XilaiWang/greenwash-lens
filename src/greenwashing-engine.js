const {
  ENGINE_VERSION,
  CLAIM_THRESHOLD,
  MIN_GREEN_CLAIM_TOKENS,
  createEmptyResult,
  sampleText,
  scoreText,
} = require("./engine-core");

module.exports = {
  ENGINE_VERSION,
  CLAIM_THRESHOLD,
  MIN_GREEN_CLAIM_TOKENS,
  createEmptyResult,
  sampleText,
  scoreText,
};
